<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.mahesh.iintent">

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/app_logo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".delete_bill"
            android:launchMode="singleTop"/>
        <activity android:name=".create_pdf"
            android:launchMode="singleTop"/>
        <activity android:name=".MainActivity"
            android:launchMode="singleTop">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <meta-data
                android:name="android.service.chooser.chooser_target_service"
                android:value=".SampleChooserTargetService" />
        </activity>
        <activity android:name=".Main2Activity"
            android:launchMode="singleTop"/>
        <activity android:name=".addnameActivity"
            android:launchMode="singleTop"/>
        <activity android:name=".ListDataActivity"
            android:launchMode="singleTop"/>
        <activity android:name=".ListDataActivity_data"
            android:launchMode="singleTop"/>
        <activity android:name=".update_bill"
            android:launchMode="singleTop"/>
        <activity android:name=".EditDataActivity"
            android:launchMode="singleTop"/>

    </application>

</manifest>